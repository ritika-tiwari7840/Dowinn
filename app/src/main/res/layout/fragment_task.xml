<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryContainer">

    <!-- Main content area -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Main vertical layout -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <!-- Top App Bar -->
            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/topAppBar"
                style="@style/Widget.Dowinn.Toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:navigationIcon="@drawable/svgrepo_iconcarrier"
                app:title="Tasks"
                app:titleTextAppearance="@style/CustomToolbarTitle" />

            <!-- Empty state content -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"

                android:orientation="vertical"
                android:gravity="center"
                android:padding="16dp">

                <ImageView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:src="@drawable/no_task_yet"
                    android:layout_marginTop="50dp"
                    android:visibility="visible" />

                <TextView
                    android:id="@+id/emptyStateText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="34dp"
                    android:text="@string/no_tasks_message"
                    android:textAlignment="center"
                    android:textColor="@color/colorOnPrimary"
                    android:textSize="16sp"
                    android:visibility="visible"
                    android:fontFamily="@font/regular"
                    />

                <TextView
                    android:id="@+id/emptyStateText2"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/no_tasks_message1"
                    android:textAlignment="center"
                    android:textColor="@color/colorOnPrimary"
                    android:textSize="16sp"
                    android:fontFamily="@font/regular"
                    android:visibility="visible" />
            </LinearLayout>
        </LinearLayout>

        <!-- Floating Action Button -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="24dp"
            android:importantForAccessibility="no"
            android:src="@drawable/add_task"
            android:backgroundTint="@color/colorPrimary"
            android:tint="@color/colorPrimary"
            android:clickable="true"
            android:focusable="true"
            app:fabSize="normal"
            app:useCompatPadding="true"
            app:tooltipText="Add Task" />
    </FrameLayout>

    <!-- Navigation Drawer -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/drawer_color"
        app:headerLayout="@layout/nav_header"
        app:itemIconTint="@color/colorPrimary"
        app:itemTextAppearance="@style/CustomNavigationItemText"
        app:itemTextColor="@color/colorOnPrimary"
        app:menu="@menu/drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>
