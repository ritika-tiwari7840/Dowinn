<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused -->
    <item android:state_focused="true" android:color="@color/borderColor"/>
    <!-- Activated -->
    <item android:state_activated="true" android:color="@color/borderColor"/>
    <!-- Pressed -->
    <item android:state_pressed="true" android:color="@color/borderColor"/>
    <!-- Hovered -->
    <item android:state_hovered="true" android:color="@color/borderColor"/>
    <!-- Default -->
    <item android:color="@color/borderColor"/>
    <item android:state_focused="true" android:color="@color/borderColor"/>
    <item android:state_activated="true" android:color="@color/borderColor"/>
    <item android:state_pressed="true" android:color="@color/borderColor"/>
    <item android:state_hovered="true" android:color="@color/borderColor"/>
    <item android:color="@color/borderColor"/>
</selector>
